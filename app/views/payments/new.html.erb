<div class="container my-5">
  <h2 class="text-center mb-4">Pay for <%= @event.title %></h2>

  <%= form_with url: pay_event_path(@event), method: :post, local: true do |form| %>
    <div class="mb-3">
      <label for="amount" class="form-label">Amount</label>
      <%= form.text_field :amount, class: "form-control", readonly: true, value: 100.0 %>
      <small class="text-muted">Amount: ₹100</small>
    </div>

    <%= form.submit "Pay Now", class: "btn btn-success w-100" %>
  <% end %>
</div>
