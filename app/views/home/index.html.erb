<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome to Event Manager</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <style>
      body {
        background-color: #f8f9fa;
        font-family: 'Arial', sans-serif;
      }
      .main-container {
        max-width: 600px;
        margin: 80px auto;
        padding: 30px;
        background-color: white;
        border-radius: 8px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      }
      .main-container h2 {
        color: #333;
        font-size: 32px;
        font-weight: 600;
        margin-bottom: 20px;
      }
      .btn-custom {
        background-color: #007bff;
        color: white;
        border-radius: 5px;
        padding: 10px 20px;
        font-size: 16px;
        width: 100%;
      }
      .btn-custom:hover {
        background-color: #0056b3;
      }
      .btn-secondary {
        background-color: #6c757d;
        color: white;
        font-size: 16px;
        padding: 10px 20px;
        border-radius: 5px;
        width: 100%;
      }
      .btn-secondary:hover {
        background-color: #5a6268;
      }
    </style>
  </head>
  <body>
    <!-- Main Content -->
    <div class="main-container">
      <h2 class="text-center">Welcome to Event Manager</h2>
      <p class="text-center mb-4">Please log in or sign up to continue.</p>

      <%= link_to "Login", new_user_session_path, class: "btn btn-custom mb-3" %>
      <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-secondary" %>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.min.js"></script>
  </body>
</html>
